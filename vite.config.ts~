import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

const repoName = 'tribecon1.github.io/'

export default defineConfig({
  base: `/${repoName}/`,
  plugins: [react()],
})

